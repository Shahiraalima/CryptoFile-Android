<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".CryptoFIleApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CryptoFile"
        tools:targetApi="31"
        tools:ignore="MissingClass">
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.auth.RegisterActivity"/>
        <activity android:name=".ui.admin.AdminHomeActivity"/>
        <activity android:name=".ui.user.UserHomeActivity"/>
        <activity android:name=".ui.encryption.EncryptFileActivity"/>
        <activity android:name=".ui.encryption.DecryptFileActivity"/>
        <activity android:name=".ui.user.UserProfileActivity"/>
        <activity android:name=".ui.user.UserActivityLogsActivity"/>
        <activity android:name=".ui.admin.AdminUserManagementActivity"/>


    </application>

</manifest>